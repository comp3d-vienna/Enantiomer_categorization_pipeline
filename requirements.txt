# Python packages for this pipeline. Prefer the conda env:
#
#   conda env create -f environment.yml
#   conda activate categorize_pipeline
#   python -m pip install -e .
#
# RDKit and BioPython are more reliable from conda-forge than from pip.
# CDPKit is not on conda-forge; install it with pip (also listed in environment.yml).
#
# Tested in categorize_pipeline (Python 3.13.15):
#   RDKit 2026.03.5, BioPython 1.88, pandas 3.0.5, numpy 2.5.2,
#   CDPKit 1.3.0, matplotlib 3.11.1, scikit-learn 1.9.0, seaborn 0.13.2

pandas>=2.0
numpy
pytz
tqdm
requests
biopython
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
CDPKit>=1.3.0
umap-learn>=0.5.12
