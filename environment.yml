name: categorize_pipeline
channels:
  - conda-forge
dependencies:
  - python=3.13
  - pip
  # Pipeline (all stages)
  - rdkit
  - biopython
  - pandas
  - numpy
  - pytz
  - tqdm
  - requests
  # Notebooks (Notebooks/*.ipynb)
  - matplotlib
  - seaborn
  - scikit-learn
  - ipykernel
  - umap-learn
  - pip:
      - CDPKit>=1.3.0
